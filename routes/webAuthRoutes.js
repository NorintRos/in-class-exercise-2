const { Router } = require('express');
const authController = require('../controllers/authController');

const router = Router();

// TODO: wire up GET /login, POST /login, GET /register, POST /register, POST /logout

module.exports = router;
